# Scriptly PC — מדריך משתמש

## התקנה

1. הרץ את `Scriptly-PC-Setup-0.10.2.exe`.
2. בחר שפה (עברית/אנגלית) — זה גם קובע את שפת ההתקנה וגם את ברירת המחדל
   הראשונית של האפליקציה עצמה (ניתן לשנות בהגדרות בכל עת).
3. בחר תיקיית יעד (ברירת מחדל: `C:\Program Files\Scriptly PC`).
4. סמן רכיבים נוספים לפי הצורך: קיצור שולחן עבודה, הצמדה לשורת המשימות,
   הפעלה אוטומטית עם Windows.
5. לחץ "התקן". בסיום, האפליקציה נפתחת אוטומטית.

> אם ההתקנה כבר רצה על המחשב, המתקין יבקש לסגור אותה לפני שממשיכים —
> זה מונע נעילת קבצים באמצע עדכון.

## הפעלה ראשונה

באפליקציה נפתח מסך הגדרה קצר אחד (עם גלילה), מחולק לסעיפים:
1. בחירת שפת ממשק
2. בחירת מיקרופון
3. הפעלת/כיבוי תכונות (סיכום אוטומטי, כותרות AI, כפתור צף)
4. בחירת ערכת נושא (בהיר/כהה/לפי מערכת)

כל שדה כבר מסומן בברירת מחדל סבירה, כך שכפתור **"דלג בינתיים"** (או
מקש **Esc**, או סגירת החלון) תמיד זמין ופשוט מקבל את מה שכבר מסומן —
לא חוסם המשך שימוש. אפשר לשנות הכל מאוחר יותר דרך **הגדרות**.

## הקלטה

- לחצו **התחל הקלטה** בחלון הראשי, או השתמשו בקיצור המקלדת הגלובלי
  (ברירת מחדל: `Ctrl+Alt+M`, ניתן לשינוי בהגדרות) מכל מקום במחשב.
- אפשר גם כפתור צף (overlay) שגוררים לכל מקום על שולחן העבודה.
- ההקלטה קולטת גם את המיקרופון וגם את קול המערכת (הצד השני בשיחה) בו-זמנית.
- לעצירה: אותו קיצור מקלדת, או כפתור **עצור** בחלון הצף.

## אחרי ההקלטה

אם "סיכום אוטומטי" מופעל (ברירת מחדל), התמלול והסיכום מתבצעים אוטומטית.
אחרת — לחצו **סכם עכשיו** ברשימת ההקלטות כשתהיו מוכנים.

לכל הקלטה: תמלול מלא, סיכום, נקודות מרכזיות, ורשימת משימות שזוהו
אוטומטית. אפשר להוסיף תגיות והערות אישיות, ולהאזין להקלטה המקורית
(אם "שמור גם קובץ קול" מסומן בהגדרות).

## ייצוא ושיתוף

- **ייצוא בודד**: כפתור ⬇ בתצוגת הקלטה — Word/Markdown/טקסט.
- **דוח מרוכז (Digest)**: תפריט ☰ → "ייצוא דוח מרוכז" — מסמך Word אחד
  שמסכם כמה הקלטות יחד (שבוע/חודש/הכל).
- **Google Tasks/Calendar** (אופציונלי, כבוי כברירת מחדל): הגדרות →
  חיבורים. דורש יצירת OAuth Client ב-Google Cloud Console (חד-פעמי,
  בחשבון Google שלכם).

## פקודות קוליות

הגדרות → הפעל "פקודות קוליות". לאחר מכן, לחצו על כפתור 🎤 — האפליקציה
מאזינה עד 60 שניות ל-"תתחיל הקלטה"/"תפסיק הקלטה" (עברית או אנגלית).
לא מאזינה תמידית — רק בחלון הזמן הזה, ביוזמתכם.

## הגדרות עיקריות

| קטגוריה | מה יש שם |
|---|---|
| 🎙 הקלטה | קיצור מקלדת, תיקיית שמירה, בחירת מיקרופון, שמירת קובץ קול |
| 🧠 תמלול וסיכום | מודל Ollama, מנוע תמלול (CPU/CUDA), מילון מונחים מותאם |
| 🎨 ממשק | שפה, ערכת נושא, גודל טקסט, הפעלה אוטומטית, כפתור צף |
| 💾 אחסון וייצוא | התראות, שמירה אוטומטית, פורמט ייצוא ברירת מחדל |
| 🔗 חיבורים | Google Tasks/Calendar |

## פתרון תקלות

תפריט ☰ → **בדיקת תקינות** בודק אוטומטית: חיבור ל-Ollama, זיהוי כרטיס
מסך (GPU), מיקרופון, מקום פנוי בדיסק, ואזהרת תיקיית סנכרון ענן (אם
תיקיית ההקלטות נמצאת בתוך OneDrive/Dropbox וכו').

לוגים: `data\scriptly.log` בתוך תיקיית ההתקנה.

## הסרת התקנה

לוח הבקרה → תוכניות → הסר Scriptly PC. תישאלו אם למחוק גם את ההקלטות
וההגדרות — ברירת המחדל היא **לא** (כלומר הן נשארות אם לא מאשרים במפורש).
