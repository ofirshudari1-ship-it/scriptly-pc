# Scriptly PC — עדכוני גרסאות

## v0.13.0 — התקנה: מ-Inno Setup לאשף התקנה מותאם-אישית ב-PyQt6 — 2026-09-17

**שינוי טכנולוגיית התקנה מלא, לא רק תיקון עיצוב.** ה-owner קבע בביקורת קודמת
(v0.12.1) ש-Inno Setup, גם עם תמונות ממותגות (navy/teal, soundwave), **תמיד**
מצייר את הכפתורים והבקרות של האשף עם ה-chrome המקורי של Windows - כי
`WizardStyle=modern` שולט רק ברקע/פריסה, לא בציור הכפתורים עצמו. זה לא עומד
בסטנדרט "בקרות ממותגות-מותג אמיתיות" שהוגדר לכל 11 הכלים. הפתרון: אשף התקנה
עצמאי לחלוטין, בנוי ב-PyQt6, באותה ארכיטקטורה בדיוק כמו ה-implementation
הקיימת וה-*עובדת* ב-`PC-Software/SnapAI/build/build_installer.py` (אותו stack
טכנולוגי בדיוק) - שם תוקן לאחרונה באג אמיתי: בלי `app.setStyle("Fusion")`,
Qt6 על Windows 11 **מתעלם בשקט** מעיצוב QSS מותאם על QPushButton, כך שהאשף
נראה עם כפתורי chrome מקוריים גם עם QSS "נכון" טכנית. `build/build_installer.py`
החדש קורא את הלקח הזה מהתחלה - `app.setStyle("Fusion")` נקרא **לפני** יצירת
כל widget.

**מה נבנה (`build/build_installer.py`, ~700 שורות, מחליף לחלוטין את
`build/installer/scriptly.iss`):**
- אשף PyQt6 עם 5 עמודים (`QStackedWidget`, לא `QWizard` - אותה החלטה
  ותיעוד כמו ב-SnapAI, בגלל באג ידוע של `QWizard.isComplete()`/Next שנתקע):
  ברוכים הבאים+הסבר → רישיון (EULA.txt האמיתי, לא MIT גנרי) → מיקום התקנה
  (+ בורר שפה EN/HE משולב) → התקדמות → סיום.
- **פלטת מותג אמיתית** מ-`assets/BRAND.md` (לא צבעי SnapAI): `NAVY=#152847`
  (header), `TEAL=#00b8ae` (accent ראשי), `TEAL_HOVER=#009a92` - כל כפתור/
  checkbox/progress bar/שדה קלט מעוצב עם QSS מפורש בצבעים האלה, לא ברירת
  מחדל של Qt.
- עמוד "ברוכים הבאים" עם קופי שיווקי אמיתי על Scriptly PC (הקלטת
  פגישות/שיחות מכל אפליקציה, תמלול Whisper מותאם עברית, סיכום+משימות עם
  Ollama מקומי, "שום דבר לא עוזב את המחשב") - לא הטקסט הגנרי של SnapAI.
- אייקון האפליקציה האמיתי (`assets/icon_idle.ico`, מוטמע ב-`--add-data` וגם
  כאייקון ה-exe עצמו דרך `--icon`) - לא סמל מצויר מחדש; ציור fallback (אותו
  soundwave mark שמצייר `build/generate_icons.py`) רק אם הקובץ חסר בזמן ריצה.
- בדיקת instance-יחיד אמיתית: פותח את ה-`Global\ScriptlyPC_SingleInstance_Mutex`
  עם `OpenMutexW` (בדיוק אותו מנגנון כמו `app/single_instance.py` ו-scriptly.iss's
  `CheckForMutexes`), לא בדיקת `tasklist` לפי שם - כך שלא יכול לפספס exe
  שהוזז/שונה שם, ולא יכול false-positive על תהליך לא קשור עם אותו שם תצוגה.
- העתקת אפליקציה גדולה (~1GB, אלפי קבצים תחת `_internal/` - Whisper+
  ctranslate2+CUDA) עם התקדמות אמיתית: `_copy_tree_with_progress` סורק קודם
  את כל הקבצים לספירה כוללת, מעתיק קובץ-קובץ עם `shutil.copy2`, ומעדכן
  progress bar + שורת סטטוס עם המספור (לא `shutil.copytree` שקט שנראה כמו
  תקיעה לדקות ארוכות).
- קיצורי דרך שולחן עבודה/תפריט התחל (checkboxes אופציונליים), הפעלה
  אוטומטית בהפעלת Windows, הפעלה מיידית בסיום ההתקנה - כולם checkboxes
  אופציונליים כמו ב-scriptly.iss הקודם.
- רשומת הסרת-התקנה ב-Registry (`HKCU\...\Uninstall\Scriptly PC`) עם
  DisplayName/Version/Publisher/InstallLocation/UninstallString/DisplayIcon.
- `--uac-admin` בזמן קומפילציית PyInstaller (מטמיע manifest) - נדרש כדי
  לכתוב ל-`C:\Program Files` כברירת מחדל, אותו טעם כמו ב-SnapAI.

**`build/build.ps1`:** שלב 5 (בעבר "Inno Setup: installer") הוחלף בקריאה
ל-`venv\Scripts\python.exe build\build_installer.py` - שאר המבנה נשאר זהה
(venv → תלויות → בדיקות → אייקונים → PyInstaller `--onedir` → **התקנה**
→ ניקוי). `build/requirements-dev.txt` קיבל `PyQt6==6.9.1` ו-`pywin32==311`
(תלויות build-only, לא runtime dependencies של האפליקציה עצמה - `app/`
ממשיך להשתמש ב-`customtkinter`, לא ב-Qt; ה-installer הוא כלי נפרד לגמרי
שרק *בונה* ומריץ עצמו כ-exe עצמאי).

`build/installer/scriptly.iss` **נשאר בקוד** (לא נמחק) כתיעוד/רפרנס היסטורי
למה שהוחלף ולמנגנון ה-mutex/registry המקורי שה-Python מדגם ממנו, אבל
`build.ps1` כבר לא קורא לו ולא תלוי ב-Inno Setup 6 מותקן.

**נבדק:** `python -m unittest discover tests` (venv) - 25/25 בדיקות עוברות
ללא שינוי (האפליקציה עצמה לא נגעה). `build.ps1` רץ מקצה לקצה ומייצר
`Scriptly-PC-Setup-0.13.0.exe` בשורש.

**לא ניתן לאמת ללא GUI חי:** מראה בפועל של העמודים (ניגודיות, יישור RTL
בעברית, גודל האשף במסך אמיתי) - נבדק קוד+QSS בלבד, לא הרצה אינטראקטיבית.

## v0.12.1 — ביקורת מותג/התקנה: אימות תמונות אשף ההתקנה מול BRAND.md — 2026-09-17

ביקורת קצרה על `build/installer/scriptly.iss` מול `assets/BRAND.md`: `WizardStyle=modern`
כבר קיים, ותמונות האשף (`assets/installer_wizard.bmp` 164x314 / `installer_wizard_small.bmp`
55x58, מיוצרות ב-`build/generate_installer_banner.py`) כבר ממותגות כראוי - גרדיאנט נייבי
(`#152847`→`#0c1424`) עם סימן ה-soundwave בטיל (`#00b8ae`), לא ברירת המחדל האפורה של
Inno. אומתו גם: בורר שפה דו-לשוני (Hebrew/English), Browse למיקום התקנה, קיצורי דרך
(שולחן עבודה/quick launch/autostart), הסרת התקנה נקייה עם מחיקת נתונים אופציונלית,
ומודעות לעדכון (AppId קבוע + UsePreviousAppDir). לא נדרשו שינויים בתמונות - רק אימות
ובנייה מחדש לוודא שההתקנה עדיין תקינה מקצה לקצה.

## v0.12.0 — רשת ביטחון זיכרון להקלטות ארוכות מאוד, וחשיפת הגבלת הגודל בהגדרות — 2026-09-15

**המשך לביקורת הצנרת (pipeline) שהתבקשה השבוע** - הקלטה+תמלול+סיכום עברו סקירת
עומס/שבריריות, לא רק פיצ'רים. `app/audio_capture.py: MeetingRecorder` שומר את כל
קטעי האודיו (מיקרופון + אודיו מערכת) בזיכרון כמערכי NumPy עד `stop()`, שרק אז ממזג
וכותב קובץ WAV אחד - עיצוב סביר להקלטת פגישה רגילה (דקות עד שעות בודדות), אבל
**הקלטה שנשארה פועלת בטעות הרבה שעות** (מחשב נרדם עם השיחה עדיין פתוחה, קיצור
מקלדת שנלחץ בטעות ונשכח) גדלה בלי גבול ויכולה להקריס את כל האפליקציה מ-Out-of-Memory
**ולאבד את כל ההקלטה** - בדיוק המקרה ש-`config.py`'s `DEFAULTS["max_file_size_gb"]`
(ברירת מחדל 10GB) תוכנן למנוע, אבל **שום קוד בשום מקום לא קרא את הערך הזה בפועל**
(אותו דפוס בדיוק כמו `min_recording_duration` שתועד ותוקן בגרסה קודמת - הגדרה
מוצהרת שאין לה שום השפעה בפועל).

**מחקר תחרותי קצר (Krisp, ספטמבר 2026):** Krisp מריץ הפחתת רעש בזמן אמת **מקומית
במכשיר** גם כשחלק אחר מהמוצר הוא ענן, אבל התוכנית החינמית שלו מגבילה שימוש ל-60
דקות/יום - כלומר יש לו תקרה מובנית שמגינה עליו מעומס בלתי מוגבל. ל-Scriptly PC
(100% מקומי, בלי תקרת שימוש עסקית) **אין** הגנה כזו מובנית מעצם המודל העסקי - כך
שרשת ביטחון טכנית בקוד עצמו היא הכרחית יותר, לא רק "נחמד שיהיה".

**חדש/תוקן:**
- 🛡 **עצירה אוטומטית בהגעה למגבלת גודל** (`app/audio_capture.py:
  MeetingRecorder.start(max_bytes=..., on_size_limit=...)`) - כל thread קורא אודיו
  מעריך את גודל החוצץ שנצבר (קירוב זול: מספר הקטעים × גודל קטע קבוע, לא סכימת בייטים
  בפועל) ועוצר את עצמו כשהוא חוצה את הסף; קריאה חוזרת (`on_size_limit`, רצה על
  thread נפרד כדי לא להיתקע ב-join() העצמי) מפעילה סיום+שמירה מיידיים במקום להשאיר
  את ה-UI מציג "מקליט..." בזמן שההקלטה בפועל כבר נעצרה ברקע בלי שהמשתמש ידע.
  `app/controller.py: start_recording()`/`_on_recording_size_limit()`/`stop_recording()`
  מזרימים את `config["max_file_size_gb"]` פנימה וגם משדרים אירוע `recording_size_limit_reached`
  שהדשבורד מציג כבאנר אזהרה עדין (לא שגיאה - ההקלטה כן נשמרה) עם הפניה להגדרות.
- ⚙ **שדה הגדרות חדש** ("גודל הקלטה מקסימלי (GB)", הגדרות → הקלטה, ליד "אורך הקלטה
  מינימלי") - עד עכשיו הערך היה קיים רק כברירת מחדל שקטה בקוד; עכשיו ניתן לשינוי
  בפועל בהתאם לכמות הזיכרון הפנויה במחשב, באותו דפוס בדיוק כמו התיקון ל-
  `min_recording_duration` בגרסה קודמת.

**נבדק:** `python -m unittest discover tests` (venv של `build.ps1`, `customtkinter`+
`python-docx` מותקנים) - 25/25 בדיקות עוברות, כולל `test_en_he_key_parity` ו-
`test_every_t_call_in_source_has_a_locale_key` (כל מחרוזת חדשה עברה דרך `t()` עם
מפתחות ב-en.json+he.json - ראו §4/§6 ב-STANDARDS.md). `build/build.ps1` רץ מקצה
לקצה (venv → תלויות → בדיקות → אייקונים → PyInstaller `--onedir` → Inno Setup)
ומייצר `Scriptly-PC-Setup-0.12.0.exe` בשורש, מחליף את הגרסה הקודמת (קובץ יחיד,
בלי כפילויות).

**מה נבדק ולא שונה (מכוון):** זיהוי "פגישה" מרקע (false-positive) מטופל כבר ע"י
`Meeting.likely_noise` (גרסה קודמת) ברמת תצוגה/סינון אחרי ההקלטה; לא נוסף חסימה
מקדימה של תחילת הקלטה לפי ניתוח אודיו בזמן אמת (VAD-gate על תחילת ההקלטה עצמה) -
זה שינוי משמעותי יותר לצנרת ההקלטה שדורש כיול זהיר (סף רגישות שגוי = מפספס פגישות
אמיתיות), מעבר להיקף 2-4 השינויים המבוקש לסבב הזה.

## v0.11.0 — פילטר "כנראה רעש רקע" לרשימת ההקלטות, ותיקון באג אמיתי בייצוא דוח מרוכז ל-SRT — 2026-09-15

**המשך ישיר למחקר התחרותי של v0.10.6** (Otter.ai/Fireflies.ai/Fathom/Granola): מעבר
לתיוג נושא אוטומטי, כל הכלים המובילים גם עוזרים למשתמש להתעלם מהקלטות "ריקות" -
בדיקת מיקרופון, שיחה שנקטעה, רעש רקע שה-VAD עדיין תפס כדיבור. בכלי ענן זה בדרך כלל
עוד מודל AI; ב-Scriptly PC (100% מקומי) זה יושם כהיוריסטיקה מקומית טהורה - **בלי שום
קריאת AI נוספת** - כדי לא להוסיף זמן עיבוד או תלות.

**חדש:**
- 🔈 **פילטר "הסתר הקלטות שנראות כמו רעש רקע"** (`app/meetings.py: Meeting.likely_noise`,
  `app/dashboard.py`) - היוריסטיקה מקומית טהורה (מספר מילים בתמלול חלקי משך ההקלטה
  בדקות < 5 מילים/דקה, ורק להקלטות מעל 25 שניות) שמזהה הקלטות שכנראה לא הכילו שיחה
  אמיתית. תיבת סימון חדשה מעל רשימת ההקלטות (כבויה כברירת מחדל, נשמרת ב-`config.json`
  כמו שאר ההעדפות) מסתירה אותן; הקלטות מסומנות תמיד ב-badge קטן ("🔇 כנראה רעש רקע")
  ברשימה גם כשהפילטר כבוי, כדי שהמשתמש יידע למה. שום דבר לא נמחק - זה מסנן תצוגה
  בלבד, הפיך תמיד. נבדק ב-5 בדיקות יחידה חדשות (`tests/test_smoke.py:
  TestLikelyNoiseHeuristic`) שמכסות הקלטה דלילה/רגילה/קצרה/ריקה/בעיבוד.
- 🎬 **ייצוא הקלטה בודדת ל-SRT** (`app/export.py: export_meeting_srt`) - פורמט כתוביות
  שמאפשר לשחזר את התמלול עם חותמות זמן אמיתיות (מ-`segments.json` שנשמר עכשיו לצד כל
  הקלטה בזמן העיבוד, `app/controller.py: _process_meeting`) ולהטמיע אותו בעורך וידאו -
  פיצ'ר סטנדרטי אצל Otter/Fireflies/Fathom. זמין דרך תפריט "⋯" ליד ההקלטה הנבחרת,
  בנפרד מהגדרת "פורמט ייצוא ברירת מחדל" (שנשארת docx/txt/md בלבד - ראו באג להלן).

**באג אמיתי שנמצא ותוקן:** דוח מרוכז (Export Digest, כמה הקלטות יחד) קרא ל-
`EXPORT_EXTENSIONS` הכללי (שכולל כעת גם `"srt": ".srt"` בעקבות הפיצ'ר החדש) כדי לקבוע
את סיומת קובץ השמירה - כך שאם המשתמש הגדיר `srt` כ"פורמט ייצוא ברירת מחדל", ייצוא
הדוח המרוכז היה שומר קובץ בשם `...srt` שמכיל בפועל בייטים של Word (`.docx`), כי
`export_digest()` עצמו נופל בחזרה ל-docx עבור `srt` (דוח מרוכז אין לו מקבילת כתוביות -
הפורמט הזה הגיוני רק להקלטה בודדת אחת). תוקן: נוסף `DIGEST_EXPORT_EXTENSIONS`
נפרד (בלי `srt`) ל-`app/dashboard.py: open_digest_export`, כך שסיומת הקובץ תמיד תואמת
את התוכן שבאמת נכתב.

**נבדק:** `python -m unittest discover tests` - 25/25 בדיקות עוברות בסביבת פיתוח עם
`customtkinter`/`python-docx`/`pyaudiowpatch` מותקנים (שלוש בדיקות הנגישות דורשות גם
`google-auth-oauthlib` המלא, שמותקן רק דרך `build.ps1`'s venv - לא נבדקו כאן בנפרד אך
לא נגעתי בקוד שהן בודקות). `build/build.ps1` רץ בפועל מקצה לקצה ומייצר
`Scriptly-PC-Setup-0.11.0.exe` בשורש (ראו לוג/דוח המשימה).

## v0.10.6 — תגיות נושא אוטומטיות (AI), תיקון הגדרת "אורך הקלטה מינימלי" שלא עשתה כלום, ותיקון מחרוזת קשיחה בייצוא כתוביות — 2026-09-15

**מחקר תחרותי קצר** (Otter.ai / Fireflies.ai / Fathom / Granola, ספטמבר 2026, ראו גם
`SPEC.md`): הפיצ'ר המשותף לכולם ש-Scriptly עדיין לא כיסה הוא סיווג/תיוג אוטומטי של
הקלטות לפי נושא ("table stakes" אצל כל הארבעה, וגם אצל כלים מקומיים דומים כמו
Meetily/Talat) - כדי שרשימת הקלטות ארוכה תישאר ניתנת לסינון/חיפוש בלי שהמשתמש יטרח
להקליד תגיות ידנית לכל הקלטה. הכלים הענניים עושים את זה מול שרת; Scriptly PC כבר יש
לו בדיוק את התשתית הדרושה מקומית (Ollama, כבר בשימוש ל-`generate_title`/`generate_tone`)
- אז זה תוסף טבעי בלי תלות חדשה ובלי לשבור את העיקרון "אפס קול/טקסט עוזב את המחשב".

**חדש:**
- 🏷 **תגיות נושא אוטומטיות** (`app/summarize.py: generate_tags()`, הגדרות → AI → "צור
  אוטומטית תגיות נושא לכל הקלטה", ברירת מחדל: פעיל) - אחרי כל תמלול+סיכום, אותו מודל
  Ollama המקומי מציע 2-4 תגיות נושא קצרות (למשל "חיוב", "חידוש מנוי") ומוסיף אותן
  להקלטה - **רק אם עדיין אין לה תגיות בכלל** (לעולם לא דורס תגיות שהמשתמש כבר קבע
  ידנית דרך דיאלוג התגיות/הערות). מזין ישירות את דשבורד התובנות הקיים ("תגיות מובילות")
  ואת החיפוש בלי שינוי נוסף בשום מקום אחר - שני המסלולים כבר קוראים את `meta.json`.
  חיווט: `app/controller.py: _process_meeting()`.

**באג אמיתי #1 שנמצא ותוקן:** `app/config.py`'s `DEFAULTS["min_recording_duration"]`
(5 שניות) הוצג כאילו הוא מגדיר את הסף למחיקה אוטומטית של הקלטות קצרות מדי (לחיצה
בטעות על קיצור המקלדת) - אבל שום קוד בשום מקום לא קרא את הערך הזה בפועל.
`app/controller.py` השתמש בקבוע קשיח נפרד (`MIN_RECORDING_SECONDS = 1.5`) שלא היה לו
שום קשר להגדרה, כך ש**שינוי ההגדרה (גם אילו הייתה לה שדה ב-UI) לא היה משנה שום
דבר בפועל** - קוד מת/מטעה, בניגוד ל-STANDARDS.md §2 ("ניקוי קוד מת"). תוקן:
`stop_recording()` קורא עכשיו בפועל את `config["min_recording_duration"]`, ונוסף שדה
הגדרות אמיתי ("אורך הקלטה מינימלי (שניות)", הגדרות → הקלטה) כדי שהערך יהיה ניתן
לשינוי בפועל, לא רק ברירת מחדל שקטה.

**באג אמיתי #2 שנמצא ותוקן:** ייצוא כתוביות (.srt) שנוסף לאחרונה
(`app/dashboard.py: _show_detail_menu`, `app/export.py: export_meeting_srt`) קרא
ל-`t("btn_export_srt")` בלי שהמפתח הוגדר בכלל ב-`locales/en.json`/`he.json` - בדיוק
דפוס הבאג שתועד ותוקן ב-v0.10.5 (מחרוזת שלא עוברת דרך `t()`, אלא שהפעם הפוך: המפתח
כן נקרא דרך `t()` אבל חסר במילון, כך ש-`t()` היה נופל בחזרה להציג את המפתח הגולמי
"btn_export_srt" למשתמש במקום טקסט תקין). נתפס על ידי
`test_every_t_call_in_source_has_a_locale_key` (הבדיקה שנוספה ב-v0.10.3 בדיוק בשביל
זה) - `python -m unittest discover tests` נכשל לפני התיקון, עבר אחריו. נוסף המפתח
לשתי השפות.

**נבדק:** 15/15 בדיקות עוברות (`python -m unittest discover tests`, כולל שלוש
בדיקות הנגישות ב-`TestAccessibility` שדורשות `customtkinter` בסביבת ה-venv של
`build.ps1`); `build/build.ps1` רץ מקצה לקצה (venv → תלויות → בדיקות → אייקונים →
PyInstaller `--onedir` → Inno Setup) ומייצר `Scriptly-PC-Setup-0.10.6.exe` בשורש.

## v0.10.5 — ביקורת תאימות מלאה מול STANDARDS.md: מחרוזות אנגלית קשיחות בהגדרות נמצאו ותוקנו — 2026-09-15

סבב ביקורת מלא מול `STANDARDS.md` (17 הסעיפים) - build.ps1 הורץ בפועל
מקצה לקצה (venv חדש → 15 בדיקות → PyInstaller → Inno Setup, ללא שגיאות),
ונסרקו: דליפות זיכרון (event listeners ב-`dashboard.py`/`tray.py`/
`overlay.py` - נמצאו תקינים, שלושתם singletons לכל חיי האפליקציה, אין
מסלול יצירה/הריסה חוזר שדורש ניקוי), דפוס `__file__`-יחסי מול `RESOURCE_ROOT`
(§11.9 בתיעוד - נבדק בכל `app/*.py`, אין הישנות מחוץ ל-`config.py` שכבר
מטפל בזה נכון), Windows Firewall (`google_integration.py` פותח שרת
OAuth זמני עם `run_local_server(port=0)` - נקשר ל-loopback בלבד, לא
מפעיל התראת חומת אש), EULA (תוכן איכותי, דו-לשוני, כבר תקין).

**באג אמיתי שנמצא ותוקן:** מסך ההגדרות (`app/dashboard.py`, `open_settings`)
הכיל **12 מחרוזות ממשק קשיחות באנגלית** שמעולם לא עברו דרך `t()` - כותרות
3 סעיפים שלמים ("Transcription & Summary", "Interface", "Storage & Export"),
תווית "Appearance:", 3 ערכי תפריט מראה (System/Light/Dark), ו-6 מחרוזות
בסעיף האחסון/ייצוא (הפעלת התראות, שמירה אוטומטית, פורמט ייצוא + טקסטי
עזר). למרות שהבדיקה `test_every_t_call_in_source_has_a_locale_key` עוברת
(היא בודקת רק ש-`t()` שנקרא כן קיים במילונים - לא תופסת מחרוזות שלא
עטופות ב-`t()` בכלל), מעבר בפועל לעברית היה משאיר את שלושת הסעיפים האלה
באנגלית - הפרה ישירה של "אפס מחרוזות קשיחות" (STANDARDS.md §4). נוספו
20 מפתחות חדשים ל-`locales/en.json`+`locales/he.json` וכל המקומות הוחלפו
ל-`t(...)`. אומת: 15/15 בדיקות עוברות כולל `test_en_he_key_parity`, ו-build
מלא רץ בהצלחה עם התיקון.

**ניקוי תיקייה:** `venv/` ו-`build_run.log` שנוצרו תוך כדי הרצת `build.ps1`
בסבב הזה הוסרו מהשורש בסיום (build-only, לא חלק מהמקור).

## v0.10.4 — נגישות: 3 כשלי ניגודיות WCAG AA אמיתיים נמצאו ותוקנו — 2026-09-15

המשך ישיר לסבב הקודם (§14.3 "ניגודיות טקסט/רקע עומדת ב-WCAG AA (4.5:1)").
במקום להסתפק ב"יש התאמה לנגישות" כללי, חושבו יחסי ניגודיות מדויקים לכל
צמד טקסט/רקע שבאמת בשימוש בקוד - **ונמצאו 3 כשלים אמיתיים**, לא תיאורטיים:

1. **כותרות בצבע טורקיז על רקע בהיר: 2.25:1** (במקום 4.5:1 נדרש) - `C["teal"]`
   שימש גם כמילוי כפתורים וגם כצבע טקסט ישירות על הרקע הבהיר (`#f1f4f9`),
   ושתי המטרות דורשות גוונים שונים. נוסף טוקן `teal_text` (`#00706a` במצב
   בהיר, זהה למצב כהה שכבר עבר) והוחלף בכל 10 המקומות שהשתמשו בטורקיז
   כ-`text_color` (כותרות הגדרות, מסכי סטטיסטיקה, onboarding).
2. **טקסט לבן על כפתור "עצור הקלטה" האדום: 3.4-3.97:1** (גם שני המצבים,
   בהיר וכהה) - האדום המקורי (`#e34848`/`#f0574f`) היה בהיר מדי בשביל
   טקסט לבן קריא עליו בגודל הכפתור (13px bold - לא "טקסט גדול" לפי
   ההגדרה של WCAG). הוכהה ל-`#b11b1b`/`#cf1b12` (6.9:1/5.5:1). נוסף גם
   `red_text` (לטקסט שגיאה ישירות על משטח, לא כמילוי כפתור) כי הדרישות
   הפוכות בין שני השימושים - אותה בעיה בדיוק ב-`app/overlay.py` (כפתור
   "עצור" ב-HUD הצף, RED שם הוכהה בנפרד ל-`#db2424`).
3. **באג ממשי, לא רק צבע: `play_btn` (נגן/עצור השמעה) לא קבע `text_color`
   בכלל כשהפך אדום** - נשאר עם `C["text"]` (נייבי כהה במצב בהיר) על רקע
   אדום, **2.44:1 בלבד**. תוקן עם `text_color` מפורש בכל שלושת המצבים
   (מנגן/רגיל/כבוי) - אותו דפוס שכבר היה נכון ב-`toggle_btn`.

**נוסף `tests/test_smoke.py: TestAccessibility`** (3 בדיקות) - מחשב יחסי
ניגודיות WCAG בפועל (נוסחת luminance רשמית, לא קירוב) על כל צמדי הטקסט/רקע
שבאמת בשימוש בקוד, כדי שכשל דומה לא יחזור בשקט. **אומת שהבדיקה תופסת באגים
אמיתיים** - הריצה מול הערכים הישנים (לפני התיקון) הייתה נכשלת בכל שלוש
הנקודות שתוארו למעלה.

## v0.10.3 — עמידה מורחבת ב-STANDARDS.md: מתקין, UX חלון/tray, ותיקוני אמת נוספים — 2026-09-14

סבב שני של עמידה בתקן, אחרי שסעיפים 12-14 נוספו ל-`STANDARDS.md` (התנהגות
חלון/tray, אימות/DPI/נגישות). כולל עוד באג אמיתי שנמצא (לא רק ארגון).

**באג אמיתי #2 שנמצא ותוקן:** `DEFAULTS["enable_analytics"]` היה `True`
למרות שהמוצר מתועד כ"opt-in, כבוי כברירת מחדל" בכל מקום אחר (SPEC.md,
README) - ואין בכלל backend לאנליטיקס שיפעל. משתמש חדש היה מקבל הגדרה
פעילה בניגוד מוחלט לתיעוד. תוקן ל-`False`, ונוסף smoke test ייעודי
(`test_analytics_defaults_off`) כ-regression guard.

**מתקין (`build/installer/scriptly.iss`):**
- **ניקוי רישום ב-הסרה** - `[Registry]` לא כלל דגלי `uninsdeletevalue`,
  כך שמפתחות הרישום (autostart, InstallLocation, Version) **נשארו אחרי
  הסרה** למרות שהיה נראה כאילו ההסרה "נקייה" (STANDARDS.md 11.5 - בדיוק
  התרחיש המתועד שם). תוקן.
- **תמונות אשף ממותגות** (`WizardImageFile`/`WizardSmallImageFile`,
  `build/generate_installer_banner.py`) - אותו לוגו (גל קול+סימן וי)
  ואותה פלטת נייבי/טורקיז מ-`assets/BRAND.md`, במקום ברירת המחדל האפורה
  של Inno Setup. עונה על הדרישה החדשה ש-"כל מסכי ההתקנה מתלבשים ברמה
  חזותית" (§7).
- **EULA קצר** (`build/EULA.txt`, עברית+אנגלית) - מקושר דרך `LicenseFile`.
  לא היה חובה בגרסה קודמת של הדרישה, אבל §11.11 ממליץ עליו כשההתקנה לא
  חתומה דיגיטלית (המקרה שלנו).
- **שורת זכויות יוצרים במסך הסיום** - נוספה דרך override של `[Messages]
  ClickFinish` (עברית+אנגלית), לא רק בתוך ה-About של האפליקציה.
- **CHANGELOG.md+README.md מותקנים בפועל** ליד ה-exe (`[Files]`) - קודם
  לא היו חלק מ-`dist/`, כך שכפתור "צפה בעדכוני גרסה" באפליקציה לא היה
  מוצא כלום אצל משתמש שהתקין דרך המתקין (רק אצל מי שהריץ מקוד מקור).

**התנהגות חלון/Tray (STANDARDS.md §12, נוסף אחרי הסבב הקודם):**
- **תפריט קליק-ימני על ה-tray**: סדר תוקן לפי §12.2 - פעולה ראשית
  (התחל/עצור הקלטה) ראשונה, קו מפריד, פתיחת דשבורד/תיקייה, קו מפריד,
  יציאה אחרונה ומופרדת. קליק שמאלי עדיין פותח את הדשבורד (לא הוחלף
  בטעות ל"התחל הקלטה" - שתי הדרישות נבדקו בנפרד כי ב-pystray הן לא
  אותו דבר: `default=` קובע קליק-שמאלי, סדר הפריטים ב-`Menu()` קובע
  את התצוגה בתפריט).
- **התראת "רץ ברקע" חד-פעמית** - `hide()` (המזעור ל-tray בלחיצת X) הציג
  קודם שום דבר; משתמשים חדשים לא ידעו שהתוכנה עדיין רצה. נוסף
  `tray_background_notice` (balloon notification, פעם אחת בלבד, דגל
  `tray_background_notice_shown` ב-config) - `AppController.notify_event()`
  חדש כנקודת כניסה ציבורית ל-UI events כאלה (במקום `_emit` הפרטי).
- **שמירת גודל/מיקום חלון בין הפעלות** (`window_geometry` ב-config,
  debounced-save + flush מיידי ב-`hide()`/`quit_app()`) - עם בדיקת
  תקינות מול גבולות המסך הנוכחי (§12.3: לא לפתוח חלון חלקית-מחוץ-למסך
  אם המשתמש ניתק מסך שני). **לא** נשמר/משוחזר מצב "ממוזער ל-tray" -
  החלון תמיד עולה גלוי בהפעלה מפורשת, כנדרש.

**DPI awareness (STANDARDS.md §14.2):** נוסף `SetProcessDpiAwareness`
(`app/main.py`, לפני יצירת כל חלון Tk) - בלעדיו Windows היה מותח את כל
הממשק כ-bitmap מטושטש ב-125%/150%/200% scaling במקום לצייר אותו native.

**Onboarding (`app/onboarding.py`) - שכתוב לעמידה בדרישות שהוחמצו בסבב
הקודם:**
- **היה 100% מחרוזות קשיחות באנגלית** - גם כשהמשתמש בחר עברית, שאר האשף
  (אחרי בורר השפה עצמו) נשאר באנגלית. עבר לגמרי ל-`t()` + `locales/`
  (17 מפתחות חדשים), עם `i18n.anchor()`/`justify()` ל-RTL.
- **אין כפתור "דלג" בכלל, ו-X לא עשה כלום** (`WM_DELETE_WINDOW` היה
  `lambda: None`) - עכשיו יש כפתור "דלג בינתיים" גלוי + `Esc` מדלג,
  ושניהם פשוט שומרים את מה שכבר מסומן (כל שדה כבר ברירת מחדל שפויה,
  כך ש"דלג" ו"התחל" שקולים בפועל).
- **מסך הפתיחה לא הסביר מה הכלי עושה** (§5: "לא רק 'ברוכים הבאים' ריק")
  - נוספה שורת הסבר קצרה לפני שממשיכים להגדרות.
- נוספה שורת © בתחתית האשף.

**About dialog (`app/dashboard.py`) - הורחב לפי §6 ("לוגו, גרסה, תאריך
build, קישור ל-changelog"):** נוסף `BUILD_DATE` (מ-`version.json`, חדש
ב-`config.py`), כפתור "צפה בעדכוני גרסה" שפותח את `CHANGELOG.md`
המותקן ב-Notepad, ושורת ©.

**כותרת חלון:** עכשיו כוללת גרסה (`Scriptly PC v0.10.3`), לא רק שם -
היה חסר למרות שה-About כבר הציג גרסה (§6 "מוזרק אוטומטית לכותרת חלון").

**בדיקות (`tests/test_smoke.py`):** נוספו 2 בדיקות - `test_analytics_defaults_off`
(regression guard על הבאג שתואר למעלה), ו-`test_every_t_call_in_source_has_a_locale_key`
(סורק את כל `app/*.py` אחרי קריאות `t("...")` ומוודא שהמפתח קיים בפועל
ב-`locales/en.json` - היה תופס באג אמיתי אם היה נשאר typo באחד המפתחות
החדשים של onboarding).

**מסמכים:** `README.md` קיבל סקשן "## English" מלא (מה/למי/התקנה/שימוש/
דרישות/פתרון-תקלות) - היה חסר לגמרי קודם (עברית בלבד), בניגוד ל-§1.
הסקשנים העמוקים יותר (מחקר תחרותי, ארכיטקטורה) נשארו עברית-בלבד במכוון -
תיעוד פנימי, לא הוראות למשתמש.

## עמידה בתקן הפרויקט (STANDARDS.md) — 2026-09-14 (ללא שינוי גרסה)

עדכון מקיף להתאמה למבנה התקני המשותף לכל 11 הכלים (`_AUDIT/STANDARDS.md`).

**מבנה תיקיות:**
- שורש נוקה: כל קבצי ה-build (`*.spec`, `build.log`, `generate_icons.py`,
  `requirements*.txt`, `installer/scriptly.iss`) עברו ל-`build/`.
- נוספו: `version.json` (מקור אמת יחיד לגרסה), `SPEC.md`, `tests/` (smoke
  tests), `locales/en.json`+`locales/he.json` (הוצאו מ-`app/i18n.py`),
  `USER-GUIDE.md`, `RELEASE-CHECKLIST.md`, `build/build.ps1`.
- שלושה קבצי תיעוד כפולים/מיותרים מסבב עבודה קודם (`DEPLOYMENT_READY.txt`,
  `IMPROVEMENTS.md`, `RELEASE_NOTES_v0102.md`) אוחדו לתוך הערך הזה ונמחקו —
  תועד ב-`_AUDIT/DELETIONS.md`.
- קובץ ההתקנה הסופי (`Scriptly-PC-Setup-0.10.2.exe`) עבר לשורש הפרויקט
  (במקום `dist_installer/`), בהתאם לדרישה שהתוצר הסופי יהיה בשורש.

**מתקין (Inno Setup) — שיפורים קריטיים:**
- **בחירת שפה בעברית+אנגלית** נוספה מחדש (`[Languages]` כלל רק אנגלית) —
  מסך ראשון, `ShowLanguageDialog=yes` תמיד (לא `auto` שמדלג בשקט).
- **בדיקת "כבר פתוח" לפני התקנה/עדכון** — נוסף `AppMutex` + קוד Pascal
  שבודק תהליך רץ ומציע לסגור לפני החלפת קבצים (היה קיים רק ברמת האפליקציה
  עצמה דרך `single_instance.py`, לא ברמת המתקין — זה בדיוק התרחיש שגורם
  לנעילת קובץ באמצע עדכון).
- `PrivilegesRequiredOverridesAllowed=dialog` — נפילה חזרה ל-per-user
  אם אין הרשאות אדמין, במקום כישלון.
- שם קובץ פלט כולל גרסה (`Scriptly-PC-Setup-{version}.exe`) כדי שלא
  יצטברו קבצי התקנה סותרים בין גרסאות.

**Splash screen:**
- נוסף מסך פתיחה (`app/splash.py`) — לוגו+שם+גרסה+פס התקדמות, נסגר לבד
  אחרי 800-1500ms, לא חוסם את טעינת שאר האפליקציה.

**i18n מבני:**
- `app/i18n.py` הפך למטען דק שקורא מ-`locales/en.json`/`locales/he.json`
  בזמן ריצה (fallback לעותק מוטמע אם הקבצים לא נמצאו — למשל בבנייה עם
  PyInstaller `--add-data`), במקום Python dict קשיח בקוד עצמו.

**באג אמיתי שנמצא ותוקן (לא רק ארגון מחדש):**
- `app/dashboard.py`, `app/tray.py`, `app/splash.py` חישבו את נתיב `assets/`
  דרך `Path(__file__).resolve().parent.parent` — דפוס שעובד בהרצה מקוד מקור
  אבל **שבור בפועל ב-build קפוא (PyInstaller)**: מודולי `app/` עצמם מתקמפלים
  לתוך ה-PKG המוטמע בקובץ ה-exe (לא נשארים כקבצים חופשיים), כך ש-`__file__`
  לא מצביע לנתיב אמיתי בדיסק. לעומת זאת קבצים שהוגשו דרך `--add-data`
  (`assets/`, `locales/`, `version.json`) כן נוחתים תחת `_internal/` אמיתי.
  תוקן על ידי `RESOURCE_ROOT` חדש ב-`app/config.py` (`sys._MEIPASS` בזמן
  frozen, נפרד מ-`PROJECT_ROOT` שממשיך להצביע ליד קובץ ה-exe עצמו עבור דאטה
  נייד של המשתמש - `data/`, `Meetings/`). **אומת בפועל** דרך סימולציית
  frozen-mode מול build אמיתי - לא רק syntax check.

**עוד שני תיקונים קטנים שנתפסו תוך כדי אימות:**
- 12 מפתחות ב-`locales/en.json` (ו-9 ב-`he.json`) עדיין אמרו "Scriptly" בלי
  "PC" (`about_body`, `menu_about`, `settings_title`, `onboarding_title`,
  `already_running` ועוד) - שרידי המיתוג הישן. תוקן לעקביות מלאה מול `app_title`.
- כותרת החלון (`root.title()`) הציגה רק "Scriptly PC" בלי מספר גרסה, בניגוד
  לדרישת §6 ("מוזרק אוטומטית לכותרת חלון/About/splash/installer") - הסיכום
  והתקציר הכילו גרסה, אבל שורת הכותרת עצמה לא. תוקן ב-`app/dashboard.py`
  (גם ב-`__init__` וגם ב-`rebuild()` שרץ בהחלפת שפה).

## תיקון RTL — 2026-09-14 (ללא שינוי גרסה)

ביקורת אוטומטית קודמת (grep על "RTL"/"RightToLeft" מילולית) דיווחה "אפס RTL
בקוד בכלל" — **זו הייתה טעות סריקה**, לא מצב אמיתי: `app/dashboard.py` (חלון
הלוח הראשי, 1453 שורות) כבר מכיל תמיכת RTL אמיתית ונרחבת (39 מקומות) — מתודות
`_anchor()`/`_justify()` פנימיות, היפוך סדר עמודות (`left_col`/`right_col`),
והיפוך צד packing — הכל מותנה ב-`i18n.get_language() == "he"`. הסריקה פספסה
את זה כי הדפוס לא כלל את המילה המילולית "RTL".

**מה שבאמת היה חסר, ותוקן עכשיו:**
- `app/onboarding.py` (אשף ההגדרה הראשונית) ו-`app/overlay.py` (סרגל ה-HUD
  הצף בזמן הקלטה) — לשניהם היה `0` שימושים בהיגיון RTL, לעומת ה-39 ב-dashboard.
- נוספו פונקציות עזר גלובליות ל-`app/i18n.py`: `is_rtl()`, `anchor()`,
  `justify()`, `pack_side()` — כתובת אחת ל"לאיזה כיוון" במקום לשכפל את
  ה-RTL logic המקומי שכבר קיים ב-dashboard.py בכל קובץ.
- `app/overlay.py`: כל קריאות ה-`.pack(side="left", ...)` בסרגל הצף (מצב
  מנוחה ומצב הקלטה) הוחלפו ב-`i18n.pack_side()` — הסרגל כולו (נקודה→כפתור→
  מיקרופון וכו') מתהפך ויזואלית בעברית.
- `app/onboarding.py` **לא תוקן** — הטקסט שם קשיח באנגלית מלכתחילה (לא עובר
  דרך `t()` בכלל), כי זה בדיוק המסך שבו המשתמש עוד לא בחר שפה. להוסיף שם
  RTL/עברית זה משימה נפרדת (כתיבת תוכן דו-לשוני, לא רק תיקון layout) —
  מתועד כפער פתוח, לא תוקן בסבב הזה.
- אומת: תחביר Python תקין (`ast.parse`) עבור `i18n.py` ו-`overlay.py`. **לא
  נבדק ויזואלית** (אין תצוגת GUI זמינה בסביבה הזו) — מומלץ הרצה אחת עם החלפת
  שפה לעברית לפני הפצה.

## ניקוי פרויקט — 2026-09-14 (ללא שינוי גרסה)

ביקורת קוד גילתה שתיקיית הפרויקט תפסה ~5.6GB, מזה ~4.6GB (`venv/` 2.4GB +
`dist/` 2.2GB + `build/` 41MB) היו קבצי ביניים הניתנים לשחזור מלא, ו-
`requirements.txt` לא היה נעול בגרסאות בכלל — ריצת `pip install` טרייה
יכלה להתקין גרסת חבילה שונה מזו שבפועל נבדקה ועובדת.

**תוקן:**
- נוצר `requirements-lock.txt` — freeze מלא (64 חבילות, כולל תלויות עקיפות) של הסביבה שבאמת נבדקה, לשחזור מדויק.
- `requirements.txt` נעל את התלויות הישירות לגרסאות ספציפיות, ותוקן חוסר אמיתי: `customtkinter` (ספריית ה-GUI עצמה!) לא הופיעה בו בכלל למרות שהיא מיובאת ישירות ב-4 קבצי מקור.
- נוסף `requirements-dev.txt` נפרד לכלי בנייה (pyinstaller וכו') שלא נדרשים כדי להריץ את האפליקציה.
- נוספו חבילות ה-CUDA (`nvidia-cublas-cu12`, `nvidia-cuda-nvrtc-cu12`, `nvidia-cudnn-cu12`) לתאוצת GPU בתמלול — אלו לא נפתרות אוטומטית ע"י pip מ-faster-whisper/ctranslate2, כך שהיו חסרות לגמרי מהתיעוד; בלעדיהן `pip install` טרי היה נותן תמלול על CPU בלבד (איטי משמעותית) בלי אזהרה.
- אומת: `pip install --dry-run -r requirements.txt` בסביבה נקייה נפתר בהצלחה, ללא התנגשויות.
- לאחר שהתלויות ננעלו ותועדו, `venv/`, `dist/` ו-`build/` נמחקו בבטחה (כולם ברי-שחזור מלא) — נפח הפרויקט ירד מ-~5.6GB ל-~953MB (רובו קובץ ההתקנה הסופי עצמו, שכולל מודל Whisper מקומי + ספריות CUDA).

## v0.10.2 — שינוי שם: Scriptly → Scriptly PC

תיקיית הפרויקט עצמה נשארה בשם "MeetScribe" על הדיסק כל הזמן הזה - ניסיון השינוי הראשון
(בתחילת הפרויקט) נכשל בגלל נעילת קובץ מתהליך פעיל, והוחלט אז שזה "קוסמטי בלבד". הפעם בוצע
בפועל: התיקייה הועתקה למיקום החדש (במקום "העברה" רגילה, כי סביבת הפיתוח עצמה החזיקה נעילה
על הנתיב הישן) ואומתה בהשוואת גודל וספירת קבצים לפני מחיקת הישן.

**שונה:**
- 📁 **שם התיקייה**: `MeetScribe` → `Scriptly PC`, בפועל על הדיסק (לא רק בקוד).
- 🏷 **מיתוג**: כל טקסט פונה-למשתמש (כותרת חלון, מגש המערכת, חלונות הודעה, שם ה-Publisher/Product בקובץ ההתקנה, קיצור הדרך) עודכן מ-"Scriptly" ל-"**Scriptly PC**".
- 📦 **קובץ ההתקנה**: `Scriptly-Setup.exe` → `Scriptly-PC-Setup.exe`. שם הקובץ המורכב (exe עצמו) בתוך ההתקנה גם שונה ל-`Scriptly PC.exe`. ה-AppId הפנימי (המזהה שווינדוס משתמש בו לזהות עדכון מול התקנה חדשה) **לא שונה בכוונה**.

**תוקן תוך כדי:** התגלה ש-`data/config.json` הקיים שמר נתיב מוחלט קשיח (`...\MeetScribe\Meetings`) לתיקיית ההקלטות - שינוי שם התיקייה היה שובר את זה בפועל (התוכנה הייתה מחפשת הקלטות במקום שכבר לא קיים). תוקן ידנית כחלק מהמעבר; `app/config.py` כבר מחשב את הנתיב הזה דינמית מברירת המחדל, כך שהבעיה לא תחזור אחרי שינוי מיקום עתידי.

**נשאר ללא שינוי בכוונה** (מזהים טכניים פנימיים, לא מיתוג פונה-למשתמש): שם קובץ הלוג (`scriptly.log`), שם חבילת הפייתון (`app`), שמות המודולים.

## v0.10.1 — ניקוי עיצובי: פחות עומס, פחות באגים ויזואליים

עבר על הממשק בעין ביקורתית אחרי ריבוי סבבי פיצ'רים מהירים - לא הוספת עוד כפתורים,
אלא הסרת עומס והאחדה.

**תוקן:**
- 🧹 **שורת הכלים בתצוגת הקלטה עמוסה מדי** - הצטברו 6 כפתורים בשורה אחת (נגן/מחיקה/העתקה/ייצוא/תגיות/תיקייה) - ברוחב חלון מינימלי (860px) זה היה עלול לגלוש או להיצמד. אוחד: פעולות משניות (העתקה, ייצוא, פתיחת תיקייה, מחיקה) עברו לתפריט גלישה "⋯" אחד; נגן ותגיות נשארו ככפתורים ישירים כי הם הכי נפוצים. מחיקה גם הורחקה בכוונה לתוך תפריט - פחות סיכוי ללחיצה בטעות.
- 🐛 **כותרות AI ארוכות היו עלולות לגלוש מרשימת ההקלטות** - כותרת שנוצרה אוטומטית (למשל "טיפול בטעות חיוב ואיום ביטול שירות") הוצגה בלי `wraplength`, בניגוד לתקציר מתחתיה שכן גולל שורות כמו שצריך. עכשיו הכותרת גם עוטפת שורות כראוי בתוך הסרגל הצדדי (300px).

## v0.10.0 — התחלה/עצירה בפקודה קולית

נבדק ברשת: מנוע זיהוי מילת-הפעלה המוביל (Porcupine) דורש חשבון/מפתח גישה חיצוני -
מנוגד לעיקרון "בלי חשבונות" של Scriptly PC. החלופה החינמית המובילה (Vosk) לא תומכת
בעברית בכלל. לכן הפיצ'ר נבנה סביב מנוע התמלול הקיים (ivrit-ai) על חלונות קוליים
קצרים, במקום להוסיף תלות חדשה.

**חדש:**
- 🎤 **פקודות קוליות להתחלה/עצירה** (כבוי כברירת מחדל, הגדרות → Interface) - כפתור 🎤 חדש בסרגל הצף שמאזין עד 60 שניות אחרי לחיצה (לא ברקע תמיד). אומרים "תתחיל הקלטה" / "תפסיק הקלטה" (גם באנגלית: "start/stop recording") והתוכנה מגיבה. נכבה אוטומטית אם לא זוהתה פקודה בזמן שנקבע.
- **מגבלה ידועה**: הזיהוי בודק קטע קול כל כ-2.5 שניות (לא מיידי כמו מילת-הפעלה ייעודית), ודורש לומר את המשפט המלא בבירור. מתאים כרגע לשימוש "נוח" (ידיים תפוסות, לא רוצים להתכופף למקלדת) יותר מאשר כתחליף מלא לקיצור המקלדת.

**תשתית:**
- `app/voice_commands.py` - מודול חדש: דגימת מיקרופון קצרה + זיהוי ביטוי מתוך תמלול
- `app/controller.py` - `arm_voice_command()` / `disarm_voice_command()`
- `app/overlay.py` - כפתור 🎤 בסרגל הצף (גם במצב מנוחה וגם בזמן הקלטה)

**נבדק:** לוגיקת זיהוי הביטויים (עברית/אנגלית, מצב start/stop, אי-זיהוי שווא) אומתה
בבדיקות ממוקדות; חיווט ה-controller (arm/disarm, בחירת מצב, ניתוב הפעלה) אומת בנפרד -
בלי להשתמש במיקרופון האמיתי או לפתוח חלונות על המסך.

## v0.9.1 — בדיקת תקינות ותיקוני ליטוש

עברתי שוב על הקוד (ניתוח סטטי + סריקת כל מחרוזות התרגום מול השימוש בפועל) כדי לתפוס פערים
שהצטברו - הפעם בלי לגעת במקלדת/עכבר/רמקולים של המחשב, רק קריאת קוד ובדיקות רקע.

**תוקן:**
- 💬 **אישור שמירת הגדרות** - שמירת הגדרות הייתה שקטה לגמרי (הדיאלוג פשוט נסגר); עכשיו מוצגת הודעת "ההגדרות נשמרו" קצרה, כמו בפעולות אחרות בתוכנה.
- 📝 **טקסט placeholder בשדה ההערות** - היה מוגדר אבל אף פעם לא חובר בפועל; עכשיו שדה ההערות (בדיאלוג תגיות/הערות) מציג טקסט רמז כשהוא ריק, ומתנקה אוטומטית בלחיצה.
- 💡 **תיוג ברור לכפתורי Google** ב"המשימות שלי" - כפתורי ✅/📅/📋 היו איקונים בלבד בלי שום הסבר; מעבר עכבר מעליהם מציג עכשיו מה כל כפתור עושה (Send to Google Tasks / Send to Calendar / Copy this task).

**נבדק:** ניתוח סטטי מלא (pyflakes) על כל הקוד - נקי. השוואה בין כל מחרוזת תרגום שמוגדרת לבין כל שימוש בפועל בקוד - איפשרה למצוא את שלושת התיקונים למעלה (מחרוזות שהוגדרו אך מעולם לא חוברו).

## v0.9.0 — האזנה להקלטה מתוך התוכנה

**חדש:**
- ▶ **כפתור נגינה** בתצוגת ההקלטה - אם "שמור קובץ קול" דלוק ויש קובץ שמע שמור, אפשר להאזין להקלטה המקורית ישירות מהדשבורד (▶ נגן / ⏹ עצור), בלי לצאת לתיקייה ולפתוח נגן חיצוני. כפתור מושבת אוטומטית אם אין קובץ שמע שמור להקלטה. מעבר להקלטה אחרת עוצר את הניגון הקודם אוטומטית.

**תוקן תוך כדי בדיקה:** כפתור העצירה לא חזר להציג "נגן" אחרי לחיצה - נתפס ותוקן לפני השחרור.

**תשתית:**
- `app/audio_player.py` - עטיפה מינימלית סביב `winsound` (מובנה ב-Windows, בלי תלות נוספת - תומך בפורמט ה-WAV שהתוכנה כבר מפיקה)

## v0.8.0 — דוח מרוכז (Digest) על כמה הקלטות יחד

**חדש:**
- 📊 **ייצוא דוח מרוכז** (☰ → Export Digest…) - מסמך Word אחד שמכסה טווח שלם של הקלטות (השבוע / 30 הימים האחרונים / הכל): תמונת מצב (סה"כ זמן, יחס דיבור, פילוח טון, תגיות מובילות), כל המשימות הפתוחות מכל ההקלטות בטווח, ואז תקציר קצר לכל הקלטה בנפרד - בלי לפתוח כל אחת ידנית. נבדק מקצה לקצה מול ההקלטות האמיתיות בתיקייה.

**תשתית:**
- `app/export.py` - `export_digest_docx()` חדש, בונה על `stats.py` ו-`tasks.py` שכבר היו קיימים

## v0.7.1 — מעבר איכות על הקוד + תיקון תקלה

עברתי על כל הקוד עם ניתוח סטטי (pyflakes) כדי לתפוס תקלות שהצטברו מסבבי הפיתוח המהירים -
לא פיצ'רים חדשים, רק חיזוק של מה שכבר קיים.

**תוקן:**
- 🐛 **תקלה אמיתית ב-`audio_capture.py`** (קיימת מאז v0.1.0): במקרה של הקלטה בלי שום התקן קול זמין, או עצירת הקלטה כשלא מוקלט כלום, הקוד היה אמור להציג הודעת שגיאה ברורה - אבל בגלל שינוי-שם (variable shadowing) בין משתנה לולאה לפונקציית התרגום `t()`, זה קרס בפועל עם `UnboundLocalError` לא ברור במקום. אומת עם בדיקה ממוקדת ששני מסלולי השגיאה מציגים כעת הודעה תקינה.
- ניקוי ייבוא מיותר ב-`export.py` ו-`google_integration.py`.

## v0.7.0 — חיבור ל-Google, טון שיחה, ועוד אפשרויות העתקה

**חדש:**
- 🔗 **חיבור אופציונלי ל-Google Tasks / Google Calendar** (הגדרות → Integrations) - שולחים משימה בודדת מתוך "המשימות שלי" ישירות ל-Google בלחיצת כפתור (✅ ל-Tasks, 📅 ליומן). כבוי כברירת מחדל, opt-in לגמרי: שום דבר לא נשלח עד שמחברים חשבון, וגם אז נשלח רק טקסט המשימה הבודדת שנבחרה - לא התמלול ולא הסיכום. החיבור דורש יצירת OAuth Client (סוג Desktop app) ב-Google Cloud Console על ידי המשתמש - השלב היחיד שקורה מחוץ ל-Scriptly PC.
- 😐 **זיהוי טון שיחה** - כל הקלטה מתויגת אוטומטית כ-חיובי / ניטרלי / מתוח, על בסיס הסיכום. שיחות "מתוחות" מסומנות באייקון ⚠ ברשימה כדי שיבלטו. מצטבר גם לפילוח בדשבורד התובנות.
- 📋 **"העתק הכל"** ברשימת המשימות - מעתיק את כל המשימות הפתוחות בבת אחת כרשימה, בנוסף לכפתור העתקה לכל משימה בנפרד.

**תשתית:**
- `app/google_integration.py` - מודול חדש: זרימת OAuth, שליחה ל-Tasks API ול-Calendar API
- `app/summarize.py` - `generate_tone()` חדש
- נוספו תלויות: `google-auth-oauthlib`, `google-api-python-client`

## v0.6.0 — דשבורד תובנות + כפתור הקלטה צף

נבנה אחרי מחקר תחרותי נוסף (Otter / Fireflies / Fathom / Granola, ספטמבר 2026) - הפער המרכזי שעלה:
ברוב הכלים המשימות "נתפסות" אבל לא זזות לשום מקום, והסיכומים נשארים באפליקציה נפרדת שאף אחד לא חוזר
אליה. ב-Scriptly זה כבר נפתר (My Tasks + חיפוש + ייצוא, מ-v0.4.0). Fireflies הכי חזק ב-dashboard
אנליטי; זה מה שנבנה בסבב הזה.

**חדש:**
- 📊 **דשבורד תובנות** (☰ → Statistics, הורחב משמעותית) - יחס דיבור "את/ה" מול "הצד השני" לאורך כל ההקלטות, גרף מגמה ל-14 הימים האחרונים, תגיות מובילות - בנוסף למדדים הבסיסיים שהיו כבר (סה"כ הקלטות, זמן כולל, וכו').
- 🔴 **כפתור הקלטה צף קבוע** על שולחן העבודה - לא רק HUD שמופיע בזמן הקלטה כמו קודם, אלא סרגל קטן שניתן לגרור לכל מקום, ומאפשר להתחיל הקלטה בלחיצה אחת בלי לפתוח את הדשבורד ובלי לזכור קיצור מקלדת. בזמן הקלטה הוא הופך אוטומטית ל-HUD עם טיימר וכפתור עצירה (כמו קודם). ניתן לכיבוי בהגדרות.

**תשתית:**
- `app/stats.py` - `compute_talk_ratio()`, `compute_daily_trend()`, `compute_top_tags()`
- `app/overlay.py` - נבנה מחדש עם שני מצבים (מנוחה/הקלטה) באותו חלון

## v0.5.0 — כותרות חכמות + נגישות

**חדש:**
- 🧠 **כותרות אוטומטיות בבינה מלאכותית** - אחרי כל סיכום, Scriptly מייצר לבד כותרת קצרה ומדויקת להקלטה (למשל "בקשת הנחה וחידוש מנוי - דנה") במקום להציג רק תאריך ושעה. אפשר לכבות בהגדרות. השעה המקורית עדיין מוצגת מתחת לכותרת.
- ♿ **גודל טקסט מתכוונן** (הגדרות → Interface → Text size: רגיל / גדול / גדול מאוד) - מגדיל את כל הממשק עבור משתמשים שזקוקים לטקסט גדול יותר.
- 💬 **מסך ריק חכם יותר** - כשאין עדיין הקלטות, ההודעה מציגה את קיצור המקלדת האמיתי שהוגדר במקום טקסט כללי.

**תשתית:**
- `app/summarize.py` - `generate_title()` חדש, קורא ל-Ollama על בסיס הסיכום
- `app/meetings.py` - שדה `ai_title` חדש ב-`Meeting`, עם נפילה חזרה לתאריך/שעה כשאין כותרת
- **תיקון**: מספר הגרסה בקובץ ההתקנה (Inno Setup) היה תקוע על 0.1.0 מאז הגרסה הראשונה ולא עודכן יחד עם `app/config.py` - תוקן, ומעכשיו שני המקומות מתעדכנים יחד בכל שדרוג. שם קובץ ההתקנה גם קובע (`Scriptly-Setup.exe`, בלי מספר גרסה בשם) כדי שלא יצטברו כמה גרסאות ישנות בתיקייה.

## v0.4.0 — פגישות ארוכות + מעקב משימות מרוכז

**חדש:**
- ✅ **"המשימות שלי"** - מסך חדש (☰ → My Tasks) שמרכז את כל המשימות שחולצו מכל ההקלטות למקום אחד, עם צ'קבוקס לסימון "בוצע" שנשמר לצמיתות (`meta.json`). אין יותר צורך לפתוח כל הקלטה בנפרד כדי לראות מה נשאר לעשות.
- ⏱ **תמיכה בפגישות ארוכות** - תמלולים ארוכים (מעל כ-3000 מילים, כ-25-30 דקות שיחה) מפוצלים אוטומטית לקטעים, כל קטע מסוכם בנפרד, ואז מוזגים לסיכום קוהרנטי אחד - במקום לחרוג מחלון ההקשר של המודל או לקבל סיכום חלקי. סגר את המגבלה שתועדה ב-README v0.3.0.

**תשתית:**
- `app/tasks.py` - מודול חדש לחילוץ וניהול משימות חוצה-הקלטות
- `app/summarize.py` - פיצול (chunking) + מיזוג (roll-up) לתמלולים ארוכים

## v0.3.0 — פיצ'רים חדשים מבוססי מחקר תחרותי

נבנה אחרי מחקר קצר על Otter.ai / Fireflies / Fathom / Granola כדי לזהות פערים (ראו [README.md §2](README.md)).

**חדש:**
- 🎙 **תיוג דוברים** ("אתה" / "הצד השני") בתמלול - מבוסס השוואת עוצמת מיקרופון מול אודיו מערכת, בלי מודל נוסף
- 🧠 **מילון מונחים מותאם** (custom vocabulary / hotwords) - משפר זיהוי שמות ומונחים מקצועיים ב-Whisper
- 🔍 **חיפוש טקסט מלא** - כולל תמלול, תגיות והערות, לא רק כותרת/תקציר
- 📅 **קיבוץ הקלטות לפי תאריך** (היום / אתמול / השבוע / קודם לכן)
- 🏷 **תגיות + הערות אישיות** לכל הקלטה (נשמר ב-`meta.json`)
- ⬇ **ייצוא ל-Word (.docx)** - לשיתוף מחוץ לתוכנה
- 🗑 **מחיקת הקלטה** (עם אישור)
- 📋 **העתקה ללוח** של סיכום/תמלול
- 📊 **מסך סטטיסטיקה** - סה"כ הקלטות, זמן כולל, השבוע, ממוצע, יום עמוס
- 🩺 **בדיקת תקינות מערכת** (System Check) - Ollama, GPU, מיקרופון, דיסק, וכתובת התיקייה, כולל אזהרת פרטיות אם התיקייה מסונכרנת לענן (OneDrive/Dropbox)
- ℹ **מסך "אודות"**

**תשתית:**
- `app/meta.py`, `app/export.py`, `app/stats.py`, `app/diagnostics.py` - מודולים חדשים
- `transcribe.py` חושף כעת timestamps לכל קטע (נדרש לתיוג דוברים)
- `audio_capture.py` שומר ציר עוצמה זמני (`speaker_timeline.json`) בזמן ההקלטה

## v0.2.0 — מיתוג, עיצוב מחדש ותיקוני יציבות

- שינוי שם: **MeetScribe → Scriptly** (ברירת מחדל אנגלית, לא רק "לפגישות")
- מנעול הפעלה יחידה (single-instance) - תיקון לבאג שגרם להתנהגות כאוטית כשכמה עותקים רצו יחד
- דשבורד מלא (CustomTkinter) עם מצב כהה/בהיר, במקום אייקון מגש בלבד
- הגנה מפני "המצאת" סיכום כשההקלטה קצרה/חסרת תוכן (למשל "בדיקה בדיקה 1 2 3")
- תיקון רינדור Markdown (**מודגש** ותבליטים) בתצוגת הסיכום
- אפשרות לכבות סיכום אוטומטי + כפתור "Summarize Now" ידני
- אשף היכרות בהפעלה ראשונה (בחירת/בדיקת מיקרופון)
- חלון צף (HUD) בזמן הקלטה עם כפתור עצירה

## v0.1.0 — גרסה ראשונה

- הקלטת מיקרופון + אודיו מערכת (WASAPI loopback) במקביל, ממוזגים
- תמלול עברית מקומי (ivrit-ai/whisper-large-v3-turbo-ct2 + faster-whisper, GPU)
- סיכום + חילוץ משימות מקומי (Ollama + aya-expanse:8b)
- קיצור מקלדת גלובלי להתחלה/עצירת הקלטה
- קובץ התקנה (PyInstaller + Inno Setup)
