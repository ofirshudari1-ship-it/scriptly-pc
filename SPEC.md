# Scriptly PC — מסמך אפיון (SPEC)

## מה הכלי עושה

Scriptly PC הוא אפליקציית שולחן עבודה ל-Windows שמקליטה שיחות/פגישות
(מיקרופון + קול מערכת בו-זמנית), מתמללת אותן אוטומטית עם מנוע Whisper
מקומי (מותאם עברית, `ivrit-ai/whisper-large-v3-turbo-ct2`), ומפיקה סיכום
ומשימות עם מודל AI מקומי דרך Ollama. הכל רץ **אופליין לגמרי** — שום קובץ
קול, תמלול או סיכום לא עוזב את המחשב, אלא אם המשתמש באופן מפורש מחבר
חשבון Google ושולח משימה בודדת (opt-in, כבוי כברירת מחדל).

## למי זה מיועד

משתמש בודד (Windows), שרוצה לתעד פגישות/שיחות עבודה בלי תלות בשירות ענן,
בלי חשבון, ובלי לשלם מנוי חודשי. תומך גם בעברית וגם באנגלית מלא (UI+RTL).

## פיצ'רים עיקריים

- הקלטת מיקרופון + קול מערכת (WASAPI loopback) בו-זמנית, עם זיהוי דובר
  אוטומטי (משווה עוצמת מיקרופון מול קול מערכת — בלי מודל נוסף).
- תמלול מקומי (faster-whisper + ivrit-ai), תומך במילון מונחים מותאם אישית.
- סיכום + חילוץ משימות אוטומטי (Ollama, מודל מקומי).
- כותרות AI אוטומטיות לכל הקלטה.
- תגיות נושא אוטומטיות (AI) לכל הקלטה - 2-4 תגיות קצרות, דרך אותו Ollama מקומי, רק
  אם המשתמש עדיין לא הוסיף תגיות ידנית (v0.10.6).
- ניתוח טון שיחה (חיובי/ניטרלי/מתוח).
- דשבורד סטטיסטיקות: יחס דיבור, מגמה ל-14 יום, תגיות מובילות.
- ייצוא ל-Word/Markdown/טקסט, כולל "דוח מרוכז" (digest) על פני כמה הקלטות, וייצוא
  הקלטה בודדת ל-SRT (כתוביות עם חותמות זמן אמיתיות, v0.11.0).
- חיפוש וקיבוץ הקלטות (היום/אתמול/השבוע/קודם לכן), תגיות והערות.
- פילטר "הסתר הקלטות שנראות כמו רעש רקע" - היוריסטיקה מקומית (בלי AI נוסף) שמזהה
  הקלטות עם תמלול דליל מדי ביחס למשך שלהן ומאפשרת להסתיר אותן מהרשימה (v0.11.0).
- רשת ביטחון זיכרון: הקלטה שנשארה פועלת בטעות שעות רבות (מחשב נרדם, קיצור מקלדת
  שנשכח) נעצרת ונשמרת אוטומטית בהגיעה לגודל מוגדר (ברירת מחדל 10GB, ניתן לשינוי
  בהגדרות → הקלטה) במקום להקריס את האפליקציה מ-Out-of-Memory (v0.12.0). ל-Krisp
  (מחקר תחרותי, ספטמבר 2026) יש תקרת שימוש עסקית (60 דק'/יום בתוכנית החינמית) שמגינה
  עליו מעומס בלתי מוגבל; ל-Scriptly PC (מקומי לגמרי, בלי תקרת שימוש) אין הגנה כזו
  מובנית, ולכן רשת הביטחון הזו ממומשת בקוד עצמו.
- חיבור אופציונלי ל-Google Tasks/Calendar (OAuth, opt-in לגמרי).
- פקודות קוליות ("תתחיל/תפסיק הקלטה") — חלון האזנה של עד 60 שניות ביוזמת
  המשתמש, לא האזנה תמידית.
- כפתור הקלטה צף (overlay) שניתן לגרור לכל מקום על שולחן העבודה.
- קיצור מקלדת גלובלי להתחלה/עצירת הקלטה מכל מקום במחשב.
- Dark/Light/System theme, גודל טקסט מתכוונן, RTL מלא בעברית.
- בדיקת תקינות מערכת (System Check): Ollama, GPU, מיקרופון, מקום בדיסק,
  אזהרת תיקיית סנכרון ענן.

## מה חסר / ידוע כלא-מיושם

- אין עדיין סנכרון ענן/גיבוי מובנה (בכוונה — הכלי offline-first).
- קישור קבצי `.scriptly` (הרשמת סיומת בקובץ ההתקנה) מוכן ברמת הקוד אבל
  אין עדיין שימוש בפועל — אין עדיין תרחיש שבו קובץ כזה נפתח מבחוץ.
- אנליטיקס שימוש (`enable_analytics` ב-config) — הדגל קיים אבל אין עדיין
  צד-שרת/מימוש בפועל; כבוי כברירת מחדל.
- בדיקת עדכון גרסה מול שרת (banner "גרסה חדשה זמינה") — עדיין לא ממומש.
- חתימה דיגיטלית על קובץ ההתקנה — לא בוצעה (שלב ידני בלבד, ראו
  `build/sign.ps1` המתועד).

## ארכיטקטורה בקצרה

```
app/
├─ main.py          נקודת כניסה: splash → controller → dashboard → tray
├─ splash.py         מסך פתיחה
├─ onboarding.py      אשף הגדרה ראשונית (פעם אחת)
├─ dashboard.py        חלון ראשי (CustomTkinter)
├─ overlay.py          HUD צף בזמן הקלטה
├─ controller.py       state מרכזי, מתאם בין רכיבים
├─ audio_capture.py     הקלטת מיקרופון+מערכת (WASAPI)
├─ transcribe.py        עטיפת faster-whisper
├─ summarize.py         עטיפת Ollama (סיכום+משימות+טון)
├─ voice_commands.py     זיהוי פקודות קוליות
├─ google_integration.py OAuth + Google Tasks/Calendar
├─ export.py             ייצוא Word/Markdown/דוח מרוכז
├─ i18n.py               טוען locales/en.json + he.json, RTL helpers
├─ config.py             הגדרות + version.json כמקור אמת לגרסה
└─ single_instance.py    Mutex ברמת האפליקציה
```

מיקום נתונים: `data/config.json` (הגדרות), `data/scriptly.log` (לוג),
`Meetings/<תאריך-שעה>/` (הקלטה+תמלול+סיכום+meta.json לכל פגישה) — הכל
בתוך תיקיית האפליקציה עצמה, כדי שהיא תישאר ניידת.

## התקנה (build/)

מ-v0.13.0: אשף התקנה מותאם-אישית ב-PyQt6 (`build/build_installer.py`),
**לא** Inno Setup — Inno תמיד מצייר כפתורי wizard עם ה-chrome המקורי של
Windows בלי קשר לתמונות המיתוג, מה שלא עומד בסטנדרט הפורטפוליו לבקרות
ממותגות-מותג אמיתיות. הארכיטקטורה זהה ל-`PC-Software/SnapAI/build/build_installer.py`
(הרפרנס העובד בפועל בפורטפוליו): סקריפט Python מוטמע מקומפל בעצמו ע"י
PyInstaller לקובץ exe קטן, שבזמן ריצה מעתיק את תיקיית ה-`--onedir` הגדולה
(`dist/Scriptly PC`, ~1GB) שנבנתה בשלב קודם. עמודי האשף: שפה (מוטמע בעמוד
מיקום ההתקנה) → ברוכים הבאים/הסבר → רישיון (`build/EULA.txt`) → מיקום
התקנה → התקדמות (עם progress אמיתי על העתקת אלפי קבצים) → סיום. `app.setStyle
("Fusion")` נקרא לפני כל widget — בלעדיו Qt6-על-Windows-11 מתעלם בשקט
מעיצוב QSS על כפתורים (לקח מ-SnapAI). `PyQt6`/`pywin32` הן תלויות build-only
(`build/requirements-dev.txt`) — לא runtime dependencies של `app/` עצמו,
שממשיך להשתמש ב-`customtkinter`. `build/installer/scriptly.iss` (Inno) נשאר
בקוד כתיעוד היסטורי, אבל `build/build.ps1` כבר לא קורא לו.
